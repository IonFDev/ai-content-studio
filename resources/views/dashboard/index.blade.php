@extends('layouts.app') @section('title','Dashboard · YouTube Studio') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">Dashboard</h1><p class="text-secondary mb-0">Vista general de producción.</p></div><a href="{{ route('projects.create') }}" class="btn btn-primary">Nuevo proyecto</a></div>
<div class="row g-3 mb-4"><div class="col-md-4"><div class="card stat-card p-3"><div class="text-secondary small">Proyectos totales</div><div class="value">{{ $projectCount }}</div></div></div><div class="col-md-4"><div class="card stat-card p-3"><div class="text-secondary small">Escenas recientes</div><div class="value">{{ $sceneCount }}</div></div></div><div class="col-md-4"><div class="card stat-card p-3"><div class="text-secondary small">Imágenes generadas</div><div class="value">{{ $imageCount }}</div></div></div></div>
<div class="card"><div class="card-header bg-white d-flex justify-content-between"><strong>Proyectos recientes</strong><a href="{{ route('projects.index') }}" class="small">Ver todos</a></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Proyecto</th><th>Estado</th><th>Escenas</th><th>Imágenes</th><th></th></tr></thead><tbody>@forelse($projects as $project)<tr><td><div class="fw-semibold">{{ $project->name }}</div><div class="small text-secondary">{{ $project->character?->name ?? 'Sin personaje' }}</div></td><td><x-status-badge :status="$project->status" /></td><td>{{ $project->scenes_count }}</td><td>{{ $project->generatedImageCount() }}</td><td class="text-end"><a href="{{ route('projects.show',$project) }}" class="btn btn-sm btn-outline-secondary">Abrir</a></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-5">No hay proyectos todavía.</td></tr>@endforelse</tbody></table></div></div>
@endsection
