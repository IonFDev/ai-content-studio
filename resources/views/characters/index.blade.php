@extends('layouts.app') @section('title','Personajes · YouTube Studio') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">Personajes</h1><p class="text-secondary mb-0">Referencias maestras reutilizables para las generaciones.</p></div><a href="{{ route('characters.create') }}" class="btn btn-primary">Nuevo personaje</a></div>
<div class="row g-3">@forelse($characters as $character)<div class="col-md-6 col-xl-4"><div class="card h-100"><div class="row g-0"><div class="col-5 p-2">@if($character->portrait_reference_path)<img class="img-fluid rounded" src="{{ route('characters.image',[$character,'portrait']) }}" alt="{{ $character->name }}">@else<div class="image-placeholder rounded h-100">Sin imagen</div>@endif</div><div class="col-7"><div class="card-body"><h2 class="h6">{{ $character->name }}</h2><p class="small text-secondary">{{ $character->description }}</p><div class="small mb-3">{{ $character->projects_count }} proyectos</div><a href="{{ route('characters.edit',$character) }}" class="btn btn-sm btn-outline-primary">Editar</a></div></div></div></div></div>@empty<div class="col-12"><div class="card p-5 text-center text-secondary">No hay personajes. Crea el Detective Stickman para empezar.</div></div>@endforelse</div>
@endsection
