@extends('layouts.app') @section('title','Editar personaje · YouTube Studio') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">{{ $character->name }}</h1><p class="text-secondary">Editar personaje y referencias maestras.</p></div><form method="POST" action="{{ route('characters.destroy',$character) }}" onsubmit="return confirm('¿Eliminar este personaje?')">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm">Eliminar</button></form></div>
<div class="card p-4"><form method="POST" enctype="multipart/form-data" action="{{ route('characters.update',$character) }}">@csrf @method('PUT')<div class="mb-3"><label class="form-label">Nombre</label><input name="name" class="form-control" value="{{ $character->name }}" required></div><div class="mb-3"><label class="form-label">Descripción</label><textarea name="description" class="form-control">{{ $character->description }}</textarea></div><div class="row g-4 mb-4"><div class="col-md-6"><label class="form-label">Character Reference Sheet</label>@if($character->reference_sheet_path)<img class="img-fluid rounded d-block mb-2" style="max-height:240px" src="{{ route('characters.image',[$character,'sheet']) }}">@endif<input type="file" name="reference_sheet" class="form-control" accept="image/png,image/jpeg,image/webp"></div><div class="col-md-6"><label class="form-label">Character Portrait Reference</label>@if($character->portrait_reference_path)<img class="img-fluid rounded d-block mb-2" style="max-height:240px" src="{{ route('characters.image',[$character,'portrait']) }}">@endif<input type="file" name="portrait_reference" class="form-control" accept="image/png,image/jpeg,image/webp"></div></div><button class="btn btn-primary">Guardar cambios</button></form></div>
@endsection
